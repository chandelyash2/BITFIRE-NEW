mutation AuthLogin($input: AuthInput) {
    authLogin(input: $input) {
        token
        user {
            _id
            userName
            status
            role
            transferStatus
            bettingStatus
            loginStep
            createdAt
        }
        error {
            message
            code
        }
    }
}
