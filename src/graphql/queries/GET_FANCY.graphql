query GetFancy($input: Int!) {
  getFancy(input: $input) {
  bettingType
    status
    runnerName
    selectionId
    marketType
    marketId
    back {
      price
      size
    }
    lay {
      price
      size
    }
  }
}