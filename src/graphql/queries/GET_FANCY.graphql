query GetFancy($eventId: Int!, $sportId: Int!) {
  getFancy(eventId: $eventId, sportId: $sportId) {
    bettingType
    status
    runnerName
    selectionId
    marketType
    marketId
    back {
      price
      size
    }
    lay {
      price
      size
    }
  }
}