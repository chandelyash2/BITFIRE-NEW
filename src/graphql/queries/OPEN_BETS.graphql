query OpenBets($input: BetEnumType) {
  openBets(input: $input) {
    _id
    userId
    eventId
    sportId
    eventName
    marketId
    selectionId
    runnerName
    bettingType
    odds
    stake
    betType
    settled
    win
    profit
    loss
    createdAt
    run
  }
}
