query GetRaceMarket($input: String!) {
  getRaceMarket(input: $input) {
    id
    name
    openDate
    selected
    minLimit
    maxLimit
    betDelay
    maxOdd
    event {
      eventId
      startTime
    }
  }
}