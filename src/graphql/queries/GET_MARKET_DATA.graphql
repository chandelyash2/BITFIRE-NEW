query GetRaceMarket($input: String!) {
  getRaceMarket(input: $input) {
    marketId
    marketName
    runners {
      runnerName
      selectionId
      status
      back {
        price
        size
      }
      lay {
        price
        size
      }
    }
  }
}