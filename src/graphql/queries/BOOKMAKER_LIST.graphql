query GetBookmakerList($input: Int!) {
  getBookmakerList(input: $input) {
    marketId
    marketName
    runners {
      selectionId
      runnerName
      status
      back {
            price
            size
          }
          lay {
            price
            size
          }
    }
  }
}