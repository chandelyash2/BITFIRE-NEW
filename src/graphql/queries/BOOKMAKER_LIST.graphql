query GetBookmakerList($input: Int!) {
  getBookmakerList(input: $input) {
    marketId
    marketName
    bettingType
    runners {
      selectionId
      runnerName
      status
      back {
        price
        size
        line
      }
      lay {
        price
        size
        line
      }
      marketStatus
      ballRunning
    }
  }
}