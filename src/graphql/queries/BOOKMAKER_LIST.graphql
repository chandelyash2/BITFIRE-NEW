query GetBookmakerList($input: Int!) {
  getBookmakerList(input: $input) {
    marketId
    marketName
    bettingType
    runners {
      runnerName
      selectionId
      marketStatus
      status
      lay {
        line
        price
        size
      }
      back {
        price
        size
        line
      }
    }
  }
}
